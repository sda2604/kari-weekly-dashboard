# 🔄 СИНХРОНИЗАЦИЯ CLAUDE И CLAUDE CODE

**Дата создания:** 27 января 2026  
**Статус:** КРИТИЧНО - обязательно к исполнению

---

## 🎯 ПРОБЛЕМА

До сегодняшнего дня Claude и Claude Code работали **параллельно и несинхронно**:

### Что было не так:

**Claude (веб-интерфейс):**
- Создавал концепции и документацию
- Предлагал структуру `input/current/` + `input/archive/`
- Описывал систему архивации
- НО: не имел прямого доступа к файлам

**Claude Code (Desktop):**
- Писал код для архивации
- Создавал скрипты `archive_input_files.py`
- НО: не активировал их
- НО: generate_dashboard.py остался на старой структуре

**Результат:**
- ❌ "Мёртвый код" в проекте
- ❌ Рассинхронизация документации и реальности
- ❌ Confusion: что работает, а что нет
- ❌ Outlook макрос частично не сработал

---

## ✅ РЕШЕНИЕ: ЕДИНЫЕ ПРАВИЛА

С сегодняшнего дня (27.01.2026) Claude и Claude Code работают **СИНХРОННО**:

### Единая рабочая директория:

```
C:\Users\salni\Desktop\Данные для Claude\WORK\2025-01-22_Автоматизация_еженедельных_отчетов\
```

**Оба имеют доступ к:**
- ✅ Чтению всех файлов проекта
- ✅ Записи в `output/`, `logs/`, `test/`
- ✅ Созданию новых скриптов в `code/`

---

### Единая структура данных:

**АКТУАЛЬНАЯ СТРУКТУРА (работает):**
```
input/
├── Отчет по приросту регионы/
│   └── По регионам.xlsx
├── Отчет по приросту аксессуаров по магазинам/
│   └── Рассылка аксессуары магазины.xlsx
└── Обувь остатки и оборачиваемость по группам товара/
    └── Отчет по оборачиваемости ТЗ регион ННВ.xlsx

output/
└── dashboard_current.html

logs/
├── outlook_macro_ГГГГММДД.log
└── telegram_send_ГГГГММДД.log

telegram_bot/
├── send_dashboard.py
└── period_parser.py

code/
└── generate_dashboard.py
```

**УСТАРЕВШАЯ КОНЦЕПЦИЯ (удалена):**
```
input/
├── current/     ← НЕ ИСПОЛЬЗУЕТСЯ
└── archive/     ← НЕ ИСПОЛЬЗУЕТСЯ

code/
└── archive_input_files.py  ← УДАЛЁН
```

---

### Единые правила обработки данных:

#### 1. Outlook макрос сохраняет файлы:
```
КУДА: input/[название отчёта]/файл.xlsx
КАК: Перезаписывает существующий файл
КОГДА: При получении письма от reports@kari.com
ЛОГ: logs/outlook_macro_ГГГГММДД.log
```

#### 2. generate_dashboard.py читает файлы:
```
ОТКУДА: input/[название отчёта]/файл.xlsx
ПРОВЕРКА: Дата модификации файла
РЕЗУЛЬТАТ: output/dashboard_current.html
ЗАПУСК: Task Scheduler в 20:00 понедельник
```

#### 3. send_dashboard.py отправляет дашборд:
```
ФАЙЛ: output/dashboard_current.html
КУДА: Telegram группа
ЛОГ: logs/telegram_send_ГГГГММДД.log
ЗАПУСК: Автоматически после generate_dashboard.py
```

---

### Единое версионирование:

**НЕТ автоматической архивации!**

Причины:
- Не нужна история для этого проекта (смотрим только текущую неделю)
- Упрощает систему
- Не создаёт "мёртвый код"

**Если нужна архивация в будущем:**
1. Сначала обсудить с пользователем
2. Внести в PROJECT_CONTEXT_KARI_DASHBOARD.md
3. Реализовать и протестировать
4. Активировать только после подтверждения

**Текущий подход:** Файлы перезаписываются каждую неделю.

---

## 🔧 ПРОЦЕСС ВНЕСЕНИЯ ИЗМЕНЕНИЙ

### Правило 1: Один источник правды

**PROJECT_CONTEXT_KARI_DASHBOARD.md** = единый источник правды

**Перед любым изменением:**
1. Claude читает PROJECT_CONTEXT_KARI_DASHBOARD.md
2. Claude Code читает PROJECT_CONTEXT_KARI_DASHBOARD.md
3. Изменения вносятся СНАЧАЛА в документацию
4. ПОТОМ реализуются в коде

### Правило 2: Синхронизация изменений

**Claude делает изменение:**
```
1. Обновляет PROJECT_CONTEXT_KARI_DASHBOARD.md
2. Создаёт файл: TASK_FOR_CODE.md
3. В файле описывает ЧТО нужно сделать
4. Пользователь передаёт задачу Claude Code
```

**Claude Code делает изменение:**
```
1. Читает PROJECT_CONTEXT_KARI_DASHBOARD.md
2. Реализует изменение в коде
3. Создаёт файл: CODE_CHANGES_LOG.md
4. Обновляет PROJECT_CONTEXT_KARI_DASHBOARD.md
```

### Правило 3: Тестирование изменений

**Любое изменение проверяется через:**
```
test\test_full_pipeline.py
```

Это гарантирует что:
- ✅ Входные файлы читаются правильно
- ✅ Дашборд генерируется
- ✅ Telegram отправка работает

---

## 📋 ЧЕКЛИСТ СИНХРОНИЗАЦИИ

### Перед началом работы:

**Claude:**
- [ ] Прочитать PROJECT_CONTEXT_KARI_DASHBOARD.md
- [ ] Проверить статус в PROJECT_STATUS.md (если есть)
- [ ] Убедиться что понимаю текущую структуру

**Claude Code:**
- [ ] Прочитать PROJECT_CONTEXT_KARI_DASHBOARD.md
- [ ] Проверить что рабочая директория правильная
- [ ] Убедиться что все пути актуальны

### После изменения:

**Claude:**
- [ ] Обновить PROJECT_CONTEXT_KARI_DASHBOARD.md
- [ ] Создать TASK_FOR_CODE.md (если нужно)
- [ ] Сообщить пользователю о статусе

**Claude Code:**
- [ ] Обновить PROJECT_CONTEXT_KARI_DASHBOARD.md
- [ ] Создать CODE_CHANGES_LOG.md
- [ ] Запустить тест: test_full_pipeline.py
- [ ] Подтвердить что всё работает

---

## 🎯 ТЕКУЩИЙ СТАТУС СИНХРОНИЗАЦИИ

**Дата проверки:** 27 января 2026

### Компоненты в синхронизации:

| Компонент | Claude | Claude Code | Синхр. |
|-----------|--------|-------------|--------|
| Рабочая директория | ✅ | ✅ | ✅ |
| Структура input/ | ✅ | ✅ | ✅ |
| Outlook макрос v4.0 | ✅ | ✅ | ✅ |
| generate_dashboard.py | ✅ | ✅ | ✅ |
| send_dashboard.py | ✅ | ✅ | ✅ |
| Логирование | ✅ | ✅ | ✅ |
| Документация | ✅ | ✅ | ✅ |

### Удалённый "мёртвый код":

- ❌ `input/current/` (концепция)
- ❌ `input/archive/` (концепция)
- ❌ `code/archive_input_files.py` (скрипт)
- ❌ Упоминания архивации в документации

---

## 🚀 СОЗДАННЫЕ ИНСТРУМЕНТЫ

### test_full_pipeline.py

**Назначение:** Полное тестирование системы

**Проверяет:**
1. ✅ Структуру проекта
2. ✅ Актуальность входных файлов
3. ✅ Логи Outlook макроса
4. ✅ Генерацию дашборда
5. ✅ Отправку в Telegram

**Запуск:**
```bash
# Из командной строки:
cd "C:\Users\salni\Desktop\Данные для Claude\WORK\2025-01-22_Автоматизация_еженедельных_отчетов"
python test\test_full_pipeline.py

# Или двойной клик:
test\RUN_TEST.bat
```

**Результат:**
- Детальный отчёт о каждом этапе
- Цветной вывод (зелёный=OK, красный=ошибка)
- Тестовая отправка дашборда

---

## 📞 ПРОТОКОЛ КОММУНИКАЦИИ

### Когда пользователь просит что-то изменить:

**ШАГ 1:** Определить КТО должен это сделать
- Документация, планирование, концепция → Claude
- Код, скрипты, автоматизация → Claude Code
- Комплексные задачи → оба (последовательно)

**ШАГ 2:** Проверить синхронизацию
- Оба читают PROJECT_CONTEXT_KARI_DASHBOARD.md
- Оба понимают текущее состояние проекта

**ШАГ 3:** Внести изменения
- Обновить документацию СНАЧАЛА
- Реализовать в коде ПОТОМ
- Протестировать через test_full_pipeline.py

**ШАГ 4:** Зафиксировать результат
- Обновить PROJECT_CONTEXT_KARI_DASHBOARD.md
- Создать лог изменений (если крупное)
- Сообщить пользователю

---

## ⚠️ КРИТИЧНЫЕ ПРАВИЛА

### ЗАПРЕЩЕНО:

❌ Создавать "мёртвый код" (код который не используется)  
❌ Документировать то чего нет в реальности  
❌ Изменять структуру данных без обновления документации  
❌ Работать параллельно без синхронизации  
❌ Пропускать тестирование после изменений  

### ОБЯЗАТЕЛЬНО:

✅ Читать PROJECT_CONTEXT перед работой  
✅ Обновлять PROJECT_CONTEXT после работы  
✅ Тестировать через test_full_pipeline.py  
✅ Следовать единой структуре данных  
✅ Логировать все действия  

---

## 🎉 ИТОГИ СИНХРОНИЗАЦИИ

**Что достигнуто 27.01.2026:**

✅ **Единая рабочая директория** - оба работают в одном месте  
✅ **Единая структура данных** - удалены несуществующие концепции  
✅ **Единые правила обработки** - все компоненты согласованы  
✅ **Инструменты тестирования** - test_full_pipeline.py  
✅ **Документация актуальна** - описывает реальное состояние  

**Следующий шаг:**
- Пользователь запускает test\RUN_TEST.bat
- Проверяет что вся цепочка работает
- Система готова к автоматической работе с понедельника!

---

**Создано:** 27 января 2026, 21:00 MSK  
**Авторы:** Claude + Claude Code (синхронно)  
**Статус:** ✅ АКТИВНО - применять немедленно
