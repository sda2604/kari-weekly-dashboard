# 📧 ИНСТРУКЦИЯ: Автосохранение Excel вложений из Outlook

**Цель:** Автоматически сохранять вложения из писем с отчётами в папку `input/`

---

## 🎯 ДВА СПОСОБА

### Способ A: Правило Outlook (простой, без программирования)
- ✅ Легко настроить через интерфейс
- ✅ Работает автоматически
- ⚠️ Сохраняет ВСЕ вложения (нужна фильтрация)

### Способ B: VBA макрос (гибкий, требует программирования)
- ✅ Точная фильтрация файлов
- ✅ Переименование файлов
- ⚠️ Требует включения макросов

**Рекомендую: Способ A для начала**

---

## 📋 СПОСОБ A: ПРАВИЛО OUTLOOK

### Шаг 1: Открой Правила

1. Открой Outlook
2. Перейди: **Файл** → **Управление правилами и оповещениями**
3. Или: **Главная** → **Правила** → **Управление правилами и оповещениями**

---

### Шаг 2: Создай новое правило

1. В окне "Правила и оповещения" нажми: **"Создать правило..."**
2. Выбери: **"Применить правило к полученным сообщениям"**
3. Нажми **"Далее"**

---

### Шаг 3: Условия (от кого письма)

**Выбери условие:**
- ✅ **"от людей или общей группы"**

**Настрой условие:**
1. Внизу кликни на подчёркнутый текст: **"людей или общей группы"**
2. Введи адрес отправителя отчётов (например: `reports@company.com`)
3. Или имя отправителя
4. Нажми **"ОК"**
5. Нажми **"Далее"**

**Дополнительно (опционально):**
- Можно добавить: **"с определёнными словами в теме"**
- Например: "отчет", "прирост", "регионы"

---

### Шаг 4: Действия (что делать с письмом)

**Выбери действие:**
- ✅ **"сохранить все вложения"**

**Настрой действие:**
1. Внизу кликни на подчёркнутый текст: **"сохранить все вложения"**
2. Откроется окно выбора папки
3. Выбери папку:
   ```
   C:\Users\salni\Desktop\Данные для Claude\WORK\2025-01-22_Автоматизация_еженедельных_отчетов\input
   ```
4. **Или создай подпапку:**
   ```
   C:\Users\salni\Desktop\Данные для Claude\WORK\2025-01-22_Автоматизация_еженедельных_отчетов\input\автосохранение
   ```
5. Нажми **"ОК"**
6. Нажми **"Далее"**

---

### Шаг 5: Исключения

Обычно не нужны, просто нажми **"Далее"**

---

### Шаг 6: Завершение

1. **Имя правила:** `Автосохранение отчётов KARI`
2. ✅ Отметь: **"Включить это правило"**
3. ✅ Отметь: **"Выполнить это правило для сообщений, находящихся в папке "Входящие""**
   _(Это применит правило к старым письмам)_
4. Нажми **"Готово"**

---

### Шаг 7: Проверка

1. Правило появится в списке
2. Нажми **"ОК"** для закрытия
3. **Тест:** Отправь себе письмо с Excel вложением
4. Проверь папку `input/` - файл должен сохраниться автоматически

---

## 🔧 СПОСОБ B: VBA МАКРОС

### Зачем нужен макрос?

- Фильтрация только определённых файлов (.xlsx, .xls)
- Сохранение в разные подпапки в зависимости от названия
- Переименование файлов
- Удаление старых файлов

---

### Шаг 1: Включи макросы в Outlook

1. **Файл** → **Параметры** → **Центр управления безопасностью**
2. **Параметры центра управления безопасностью...**
3. **Параметры макросов**
4. Выбери: ⚪ **"Уведомление обо всех макросах"**
5. Нажми **"ОК"**

---

### Шаг 2: Открой редактор VBA

1. Нажми `Alt + F11` в Outlook
2. Откроется редактор VBA

---

### Шаг 3: Вставь макрос

1. В левой панели: **Project1** → **Microsoft Outlook Objects**
2. Двойной клик на: **"ThisOutlookSession"**
3. Вставь этот код:

```vba
Private Sub Application_NewMailEx(ByVal EntryIDCollection As String)
    ' Автосохранение вложений из писем с отчётами KARI
    
    Dim objMail As Outlook.MailItem
    Dim objAttachment As Outlook.Attachment
    Dim strSavePath As String
    Dim strFileName As String
    
    ' Путь для сохранения
    strSavePath = "C:\Users\salni\Desktop\Данные для Claude\WORK\2025-01-22_Автоматизация_еженедельных_отчетов\input\"
    
    On Error Resume Next
    Set objMail = Application.Session.GetItemFromID(EntryIDCollection)
    
    ' Проверка отправителя или темы
    If InStr(1, objMail.Subject, "отчет", vbTextCompare) > 0 Or _
       InStr(1, objMail.Subject, "прирост", vbTextCompare) > 0 Or _
       InStr(1, objMail.Subject, "регионы", vbTextCompare) > 0 Then
        
        ' Перебор вложений
        For Each objAttachment In objMail.Attachments
            strFileName = objAttachment.FileName
            
            ' Фильтр: только Excel файлы
            If LCase(Right(strFileName, 5)) = ".xlsx" Or _
               LCase(Right(strFileName, 4)) = ".xls" Then
                
                ' Определяем подпапку по названию
                If InStr(1, strFileName, "регионы", vbTextCompare) > 0 Then
                    objAttachment.SaveAsFile strSavePath & "Отчет по приросту регионы\" & strFileName
                ElseIf InStr(1, strFileName, "аксессуар", vbTextCompare) > 0 Then
                    objAttachment.SaveAsFile strSavePath & "Отчет по приросту аксессуаров по магазинам\" & strFileName
                ElseIf InStr(1, strFileName, "оборач", vbTextCompare) > 0 Then
                    objAttachment.SaveAsFile strSavePath & "Обувь остатки и оборачиваемость по группам товара\" & strFileName
                Else
                    ' Сохраняем в корень input/
                    objAttachment.SaveAsFile strSavePath & strFileName
                End If
            End If
        Next objAttachment
    End If
    
    Set objMail = Nothing
End Sub
```

4. Нажми `Ctrl + S` для сохранения
5. Закрой редактор VBA

---

### Шаг 4: Перезапусти Outlook

1. Закрой Outlook полностью
2. Открой снова
3. Если появится предупреждение о макросах → **"Включить макросы"**

---

### Шаг 5: Тест

1. Отправь себе тестовое письмо с Excel вложением
2. В теме напиши: "Тест отчет"
3. Проверь папку `input/` - файл должен сохраниться

---

## ⚙️ НАСТРОЙКА МАКРОСА

### Изменить путь сохранения:

Найди в коде строку:
```vba
strSavePath = "C:\Users\salni\Desktop\..."
```

Измени на свой путь.

### Добавить другие ключевые слова:

Найди строку:
```vba
If InStr(1, objMail.Subject, "отчет", vbTextCompare) > 0 Or _
```

Добавь свои слова:
```vba
If InStr(1, objMail.Subject, "отчет", vbTextCompare) > 0 Or _
   InStr(1, objMail.Subject, "прирост", vbTextCompare) > 0 Or _
   InStr(1, objMail.Subject, "ТВОЁ_СЛОВО", vbTextCompare) > 0 Then
```

### Фильтровать по отправителю:

Добавь после проверки темы:
```vba
If objMail.SenderEmailAddress = "reports@company.com" Then
```

---

## 📊 СРАВНЕНИЕ СПОСОБОВ

| Критерий | Правило Outlook | VBA Макрос |
|----------|----------------|------------|
| Настройка | ✅ Легко | ⚠️ Средне |
| Гибкость | ⚠️ Базовая | ✅ Полная |
| Фильтрация | ⚠️ По теме/отправителю | ✅ По любым критериям |
| Подпапки | ❌ Нет | ✅ Да |
| Переименование | ❌ Нет | ✅ Да |
| Безопасность | ✅ Безопасно | ⚠️ Нужны макросы |

---

## 🎯 РЕКОМЕНДАЦИЯ

**Для начала:** Используй **Правило Outlook** (Способ A)
- Быстро настраивается
- Работает сразу
- Не требует программирования

**Позже:** Если нужна гибкость → добавь **VBA макрос** (Способ B)

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Правило не срабатывает

**Проверь:**
1. Правило включено (зелёная галочка)
2. Отправитель/тема совпадают с условиями
3. Папка `input/` существует
4. Есть права на запись в папку

### Макрос не работает

**Проверь:**
1. Макросы включены
2. Код сохранён в ThisOutlookSession
3. Outlook перезапущен
4. При запуске Outlook нажал "Включить макросы"

### Файлы сохраняются с неправильными именами

**Решение:**
- Используй VBA макрос
- Добавь переименование в код

---

## ✅ ГОТОВО!

После настройки:
- ✅ Письма с отчётами приходят
- ✅ Вложения автоматически сохраняются в `input/`
- ✅ Не нужно скачивать вручную

---

**Создано:** 23 января 2026  
**Версия:** 1.0
