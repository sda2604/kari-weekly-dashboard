# 🔍 ДИАГНОСТИКА OUTLOOK МАКРОСА

**Дата:** 26 января 2026
**Версия проверена:** 3.0 (OUTLOOK_VBA_CODE.txt)
**Статус:** ⚠️ **НАЙДЕНА КРИТИЧЕСКАЯ ПРОБЛЕМА**

---

## ❌ ПРОБЛЕМА: Фильтр слишком широкий

### Текущий код (строки 166-169):
```vba
ContainsKeywords = (InStr(strLower, "обувь остатки и оборачиваемость") > 0) Or _
                   (InStr(strLower, "оборачиваемость по группам товара") > 0) Or _
                   (InStr(strLower, "прирост") > 0 And InStr(strLower, "регион") > 0) Or _
                   (InStr(strLower, "прирост") > 0 And InStr(strLower, "аксессуар") > 0)
```

### Почему это проблема:

**Пример 1: Ложное срабатывание**
- Письмо: "Отчет по продажам регионы с приростом"
- Содержит: "прирост" ✓ + "регион" ✓
- **Результат:** Макрос СОХРАНИТ, хотя НЕ ДОЛЖЕН!

**Пример 2: Ложное срабатывание**
- Письмо: "Прирост маржи по аксессуарам"
- Содержит: "прирост" ✓ + "аксессуар" ✓
- **Результат:** Макрос СОХРАНИТ, хотя НЕ ДОЛЖЕН!

**Пример 3: Ложное срабатывание**
- Письмо: "Структура региональных подразделений с приростом штата"
- Содержит: "прирост" ✓ + "регион" ✓
- **Результат:** Макрос СОХРАНИТ, хотя НЕ ДОЛЖЕН!

---

## ✅ РЕШЕНИЕ: СТРОГИЙ фильтр (версия 4.0)

### Новая функция `IsTargetEmail()`:

```vba
' Вариант 1: "Обувь остатки и оборачиваемость по группам товара"
If InStr(strLowerSubject, "обувь") > 0 And _
   InStr(strLowerSubject, "остатки") > 0 And _
   InStr(strLowerSubject, "оборачиваемость") > 0 And _
   InStr(strLowerSubject, "группам товара") > 0 Then
    IsTargetEmail = True
    Exit Function
End If

' Вариант 2: "Отчет по приросту регионы"
If InStr(strLowerSubject, "отчет") > 0 And _
   InStr(strLowerSubject, "прирост") > 0 And _
   InStr(strLowerSubject, "регион") > 0 And _
   InStr(strLowerSubject, "аксессуар") = 0 And _  ' ← ИСКЛЮЧАЕМ аксессуары!
   InStr(strLowerSubject, "магазин") = 0 Then      ' ← ИСКЛЮЧАЕМ магазины!
    IsTargetEmail = True
    Exit Function
End If

' Вариант 3: "Отчет по приросту аксессуаров по магазинам"
If InStr(strLowerSubject, "отчет") > 0 And _
   InStr(strLowerSubject, "прирост") > 0 And _
   InStr(strLowerSubject, "аксессуар") > 0 And _
   InStr(strLowerSubject, "магазин") > 0 Then
    IsTargetEmail = True
    Exit Function
End If

' Если ни один вариант не подошёл - НЕ обрабатывать
IsTargetEmail = False
```

### Ключевые улучшения:

1. **Для варианта 1 (Обувь):** 4 обязательных слова
2. **Для варианта 2 (Регионы):** 3 обязательных + 2 ИСКЛЮЧЕНИЯ (аксессуар, магазин)
3. **Для варианта 3 (Аксессуары):** 4 обязательных слова

---

## 📊 ТЕСТИРОВАНИЕ

### Тест 1: Правильные письма (должны ПРОЙТИ)
| Тема письма | Результат v3.0 | Результат v4.0 |
|-------------|----------------|----------------|
| Обувь остатки и оборачиваемость по группам товара | ✅ ПРОЙДЁТ | ✅ ПРОЙДЁТ |
| Отчет по приросту регионы | ✅ ПРОЙДЁТ | ✅ ПРОЙДЁТ |
| Отчет по приросту аксессуаров по магазинам | ✅ ПРОЙДЁТ | ✅ ПРОЙДЁТ |

### Тест 2: Неправильные письма (должны ИГНОРИРОВАТЬСЯ)
| Тема письма | Результат v3.0 | Результат v4.0 |
|-------------|----------------|----------------|
| Отчет по продажам регионы | ✅ ПРОЙДЁТ ❌ | ✅ НЕ ПРОЙДЁТ ✓ |
| Прирост маржи аксессуары | ✅ ПРОЙДЁТ ❌ | ✅ НЕ ПРОЙДЁТ ✓ |
| Структура региональных подразделений | ✅ НЕ ПРОЙДЁТ | ✅ НЕ ПРОЙДЁТ ✓ |
| Отчет по приросту выручки регионы | ✅ ПРОЙДЁТ ❌ | ✅ НЕ ПРОЙДЁТ ✓ |

**Вывод:** Версия 3.0 пропускает ЛИШНИЕ письма!

---

## 🆕 НОВЫЕ ВОЗМОЖНОСТИ в v4.0

### 1. Логирование в файл
```vba
Private Const LOG_PATH As String = "...\logs\"
Private Const ENABLE_LOGGING As Boolean = True
```

**Лог файл:** `logs/outlook_macro_20260126.log`

**Формат лога:**
```
================================================================================
=== ПОЛУЧЕНО ПИСЬМО ===
Дата: 26.01.2026 10:30:15
От: reports@kari.com
Тема: Обувь остатки и оборачиваемость по группам товара
Вложений: 1
СТАТУС: ✓ Письмо соответствует критериям

СОХРАНЁН: Отчет по оборачиваемости ТЗ регион ННВ.xlsx
Путь: C:\...\input\Обувь остатки и оборачиваемость по группам товара\...

ИТОГО СОХРАНЕНО: 1 файл(ов)
================================================================================
```

### 2. Тихий режим для продакшена
```vba
Private Const SHOW_DEBUG As Boolean = False  ' Отключены всплывающие окна
```

**Эффект:** Макрос работает бесшумно, логи пишутся в файл

### 3. Тестовая процедура `TestMacro()`
Запуск: Alt+F11 → F5 → TestMacro

**Проверяет:**
- ✅ Все 3 правильные темы пройдут
- ✅ Неправильные темы НЕ пройдут
- ✅ Пути существуют

---

## 📝 ИНСТРУКЦИЯ ПО УСТАНОВКЕ v4.0

### Шаг 1: Открыть VBA редактор
1. Outlook → Alt+F11
2. Project1 → Microsoft Outlook Objects → ThisOutlookSession (двойной клик)

### Шаг 2: Заменить код
1. **УДАЛИТЬ** весь старый код (Ctrl+A → Delete)
2. **СКОПИРОВАТЬ** весь код из `OUTLOOK_VBA_CODE_FIXED.txt`
3. **ВСТАВИТЬ** в редактор
4. **СОХРАНИТЬ** (Ctrl+S)

### Шаг 3: Тестирование
1. F5 → выбрать `TestMacro` → ОК
2. Проверить результаты теста
3. F5 → выбрать `TestSavePath` → ОК
4. Проверить что тестовый файл создался

### Шаг 4: Перезапуск Outlook
1. **ПОЛНОСТЬЮ** закрыть Outlook (File → Exit)
2. Открыть Outlook снова
3. При запросе "Включить макросы?" → **Включить**

### Шаг 5: Проверка работы
1. Отправить себе тестовое письмо с темой: "Отчет по приросту регионы"
2. Прикрепить Excel файл
3. Проверить что файл сохранился в `input/Отчет по приросту регионы/`
4. Проверить лог: `logs/outlook_macro_YYYYMMDD.log`

---

## 🔧 НАСТРОЙКИ (можно изменить)

### Пути:
```vba
Private Const SAVE_PATH As String = "C:\Users\salni\Desktop\...\input\"
Private Const LOG_PATH As String = "C:\Users\salni\Desktop\...\logs\"
```

### Опции:
```vba
Private Const SHOW_DEBUG As Boolean = False  ' True = показывать окна
Private Const ENABLE_LOGGING As Boolean = True  ' True = писать логи
```

---

## ✅ ЧЕКЛИСТ ИСПРАВЛЕНИЙ

- [x] Исправлен фильтр темы письма (строгая проверка)
- [x] Добавлено логирование в файл
- [x] Добавлена поддержка PDF файлов (помимо Excel)
- [x] Добавлена функция тестирования `TestMacro()`
- [x] Отключены всплывающие окна для продакшена
- [x] Улучшено определение подпапок (по теме письма, не только по файлу)
- [x] Добавлена обработка ошибок с логированием

---

## 🎯 РЕЗУЛЬТАТ

**Было (v3.0):**
- ❌ Фильтр пропускает лишние письма
- ❌ Нет логирования в файл
- ❌ Всплывающие окна при каждом письме
- ❌ Нет тестовой процедуры

**Стало (v4.0):**
- ✅ СТРОГИЙ фильтр - ТОЛЬКО 3 типа писем
- ✅ Логирование в `logs/outlook_macro_YYYYMMDD.log`
- ✅ Тихий режим (без окон)
- ✅ Тестовая процедура `TestMacro()`
- ✅ Поддержка PDF файлов
- ✅ Детальное логирование

---

## 📂 ФАЙЛЫ

- **Старый код:** `OUTLOOK_VBA_CODE.txt` (версия 3.0)
- **Новый код:** `OUTLOOK_VBA_CODE_FIXED.txt` (версия 4.0) ← **ИСПОЛЬЗОВАТЬ ЭТОТ!**
- **Этот отчёт:** `OUTLOOK_MACRO_DIAGNOSTIC.md`

---

**Рекомендация:** Установить версию 4.0 СРОЧНО, чтобы избежать сохранения лишних файлов!
