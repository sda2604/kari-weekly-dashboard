# ФИНАЛЬНЫЕ ДАННЫЕ ДЛЯ ДАШБОРДА - ИНСТРУКЦИЯ

## 📁 Файлы с данными

### 🎯 ОСНОВНЫЕ ФАЙЛЫ (используйте эти):

1. **final_dashboard_data.json** ⭐ ГЛАВНЫЙ ФАЙЛ
   - Полный набор данных для дашборда
   - Содержит: структуру магазинов, продажи, анализ
   - Размер: ~50 категорий с детальными метриками
   - **Используйте этот файл для создания дашборда**

2. **dashboard_summary.json** ⭐ БЫСТРЫЙ ДОСТУП
   - Краткая сводка ключевых метрик
   - Алерты о проблемах (затоваривание/дефицит)
   - ТОП-10 по влиянию, росту, продажам
   - **Используйте для превью и KPI виджетов**

3. **ФИНАЛЬНЫЙ_ОТЧЕТ_ДЛЯ_ДАШБОРДА.md** ⭐ ЧИТАЕМЫЙ ОТЧЁТ
   - Полный анализ в человекочитаемом формате
   - Таблицы, инсайты, рекомендации
   - **Используйте для презентации и документации**

---

## 📊 Структура данных

### final_dashboard_data.json

```json
{
  "structure": {
    "region": "ННВ",
    "total_shops": 119,
    "subdivisions": [
      {"name": "Нижний Новгород 1", "shops_count": 28},
      ...
    ]
  },
  "sales_data": {
    "total_categories": 133,
    "period": "12.01.2026 - 18.01.2026",
    "categories_top50": [
      {
        "category": "Женская обувь → 257 Ботинки женские зимние",
        "direction": "Женская обувь",
        "ostatok_pairs": 237.9,
        "sales_share_pct": 10.2,
        "growth_pct": 59.4,
        "impact_score": 0.0606,
        "k_ratio": 23.32
      },
      ...
    ]
  },
  "analysis": {
    "top_growth_categories": [...],
    "top_sales_share": [...],
    "top_impact": [...]
  }
}
```

### dashboard_summary.json

```json
{
  "meta": {
    "region": "ННВ",
    "period": "12.01.2026 - 18.01.2026",
    "total_shops": 119,
    "total_categories": 133
  },
  "alerts": {
    "overstocked": [
      {
        "category": "...",
        "k_ratio": 405.1,
        "issue": "Критическое затоваривание"
      }
    ],
    "low_stock": [...]
  },
  "top10_by_impact": [...],
  "growth_leaders": [...],
  "sales_leaders": [...]
}
```

---

## 🎨 Как использовать для дашборда

### 1. HTML Дашборд с Chart.js

```html
<!DOCTYPE html>
<html>
<head>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
    <canvas id="myChart"></canvas>

    <script>
        // Загружаем данные
        fetch('final_dashboard_data.json')
            .then(response => response.json())
            .then(data => {
                const categories = data.sales_data.categories_top50;

                // График ТОП-10
                new Chart(document.getElementById('myChart'), {
                    type: 'bar',
                    data: {
                        labels: categories.slice(0, 10).map(c => c.category),
                        datasets: [{
                            label: 'Доля в продажах (%)',
                            data: categories.slice(0, 10).map(c => c.sales_share_pct),
                            backgroundColor: 'rgba(54, 162, 235, 0.5)'
                        }]
                    }
                });
            });
    </script>
</body>
</html>
```

### 2. Алерты о проблемах

```javascript
fetch('dashboard_summary.json')
    .then(response => response.json())
    .then(data => {
        // Показываем критические проблемы
        data.alerts.overstocked.forEach(item => {
            console.warn(`🔴 ЗАТОВАРИВАНИЕ: ${item.category}`);
            console.log(`   Остатки: ${item.ostatok_pairs} пар`);
            console.log(`   K-коэф: ${item.k_ratio} недель`);
        });

        // Показываем риски дефицита
        data.alerts.low_stock.forEach(item => {
            console.warn(`🟡 РИСК ДЕФИЦИТА: ${item.category}`);
            console.log(`   Рост: +${item.growth_pct}%`);
        });
    });
```

### 3. Визуализация структуры магазинов

```javascript
fetch('final_dashboard_data.json')
    .then(response => response.json())
    .then(data => {
        const subdivisions = data.structure.subdivisions;

        // Pie chart по подразделениям
        new Chart(ctx, {
            type: 'pie',
            data: {
                labels: subdivisions.map(s => s.name),
                datasets: [{
                    data: subdivisions.map(s => s.shops_count),
                    backgroundColor: [
                        '#FF6384', '#36A2EB', '#FFCE56',
                        '#4BC0C0', '#9966FF', '#FF9F40', '#C9CBCF'
                    ]
                }]
            }
        });
    });
```

---

## 🔑 Ключевые метрики

### Регион ННВ:
- **119 магазинов** в 7 подразделениях
- **133 категории** товаров
- **100% охват** продаж
- **Период:** 12.01.2026 - 18.01.2026

### ТОП-3 категории по влиянию:
1. Ботинки женские зимние (257) - 10.2% продаж, +59.4%
2. Ботинки мужские зимние (261) - 8.6% продаж, +51.8%
3. Ботинки мужские зимние для активного отдыха (797) - 5.8% продаж, +50.6%

### Критические проблемы:
- **3 категории** с критическим затовариванием (K > 400 недель)
- **2 категории** с риском дефицита (K < 20 недель при росте > 50%)

---

## ⚙️ Поля данных

### Описание полей в categories:

| Поле | Тип | Описание | Пример |
|------|-----|----------|--------|
| `category` | string | Полное название категории | "Женская обувь → 257 Ботинки женские зимние" |
| `direction` | string | Направление | "Женская обувь" |
| `ostatok_pairs` | number | Остатки в парах | 237.9 |
| `sales_share_pct` | number | Доля в продажах (%) | 10.2 |
| `growth_pct` | number | Прирост к прошлой неделе (%) | 59.4 |
| `impact_score` | number | Влияние = доля × |прирост| | 0.0606 |
| `k_ratio` | number | K-коэффициент оборачиваемости | 23.32 |

### Интерпретация K-коэффициента:

| K-коэффициент | Статус | Цвет | Действие |
|---------------|--------|------|----------|
| 0-10 | Дефицит | 🔴 Красный | Срочно заказать |
| 10-20 | Низкий запас | 🟡 Жёлтый | Мониторить |
| 20-40 | Норма | 🟢 Зелёный | OK |
| 40-100 | Избыток | 🟠 Оранжевый | Снизить заказы |
| 100+ | Критический избыток | 🔴 Красный | Распродажа |

---

## 📌 Источники данных

**Исходные файлы:**
1. `input/Структура розница 21.01.2026.xlsx`
   - Структура магазинов по подразделениям
   - 119 магазинов региона ННВ

2. `input/Отчет по приросту регионы/По регионам.xlsx`
   - Данные по продажам и остаткам
   - Прирост к прошлой неделе
   - 133 категории товаров

**Дата извлечения:** 22.01.2026

---

## ✅ Готово к использованию

Используйте **final_dashboard_data.json** для создания интерактивного дашборда.
Используйте **dashboard_summary.json** для быстрых метрик и алертов.
Используйте **ФИНАЛЬНЫЙ_ОТЧЕТ_ДЛЯ_ДАШБОРДА.md** для презентаций.

**Следующий шаг:** Создать HTML дашборд с визуализацией этих данных.
