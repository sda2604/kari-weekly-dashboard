# CLAUDE.md - Контекст для AI-ассистента

## 📋 О ПРОЕКТЕ

**Название:** Автоматизация еженедельных отчётов KARI  
**Владелец:** Дмитрий Сальников, Региональный директор ННВ  
**Статус:** ✅ В production с 24.01.2026  
**Репозиторий:** https://github.com/sda2604/kari-weekly-dashboard

---

## 🎯 ЦЕЛЬ ПРОЕКТА

Автоматизация обработки еженедельных отчётов из корпоративной почты:
1. Outlook VBA макрос → автосохранение вложений из писем
2. Python скрипты → анализ Excel файлов
3. HTML дашборд → mobile-friendly отчёт с выводами
4. Telegram Bot → автоматическая рассылка в группу

**Результат:** 8 часов ручной работы → 5 минут автоматики (экономия 384 ч/год)

---

## 🏗️ АРХИТЕКТУРА

### Компоненты системы

1. **Outlook VBA макрос** (`outlook_vba/`)
   - Перехватывает письма с отчётами (каждый понедельник)
   - Сохраняет вложения в `input/`
   - Логирование в `logs/outlook_macro_*.log`

2. **Data Processor** (`generate_dashboard.py`)
   - Парсит 3 Excel файла (регионы, аксессуары, оборачиваемость)
   - Анализирует по бизнес-логике (КОП, тренды, проблемы)
   - Генерирует HTML дашборд в `output/dashboard_current.html`

3. **Telegram Bot** (`telegram_bot/`)
   - Отправляет дашборд в группу "Merchandising NNV"
   - Конфигурация в `telegram_bot/config.py` (не в git!)
   - Поддержка рассылки по списку получателей

### Запуск полного цикла

```bash
run_full_pipeline.bat
```

Последовательность:
1. `generate_dashboard.py` → создаёт дашборд
2. `telegram_bot/send_dashboard.py` → отправляет в Telegram

---

## 📊 БИЗНЕС-ЛОГИКА АНАЛИЗА

### Ключевые метрики

**КОП (Коэффициент Остатки/Продажи):**
```
КОП = Доля в остатках (%) / Доля в продажах (%)

КОП 0.8-1.2  → ✅ Баланс
КОП 1.2-2.0  → ⚠️ Затоваривание
КОП >2.0     → 🔴 Критичное затоваривание
КОП <0.8     → 🔴 Дефицит
```

**Оборачиваемость:**
- <30 недель → норма
- 30-100 недель → требует внимания
- >100 недель → мёртвый груз (распродать)

**Сезонность (январь):**
- Зима = СЕЗОН (приоритет)
- Лето = несезон (низкий приоритет)

### Приоритеты анализа

1. **Денежные коровы** (доля >3%) → ЗАЩИТИТЬ
2. **Растущие звёзды** (доля <5%, рост >50%, КОП <1.0) → НЕ УПУСТИТЬ
3. **Мёртвый груз** (оборачиваемость >50 нед) → РАСПРОДАТЬ
4. **Дисбалансы** (перераспределить между подразделениями)
5. **Магазины** (ТОП-10 vs ХУДШИЕ-10)

---

## 🗂️ СТРУКТУРА РЕГИОНА ННВ

**119 магазинов в 7 подразделениях:**

| Подразделение | Магазинов |
|--------------|-----------|
| ННВ 1 | 28 |
| Казань 1 | 23 |
| Владимир | 18 |
| Ярославское | 15 |
| Наб.Челны | 14 |
| ННВ Север | 12 |
| Ижевское | 9 |

**11 регионов компании KARI:**
ИРК, СИБ, ННВ, САМ, УРЛ, ВРН, СПБ, МСК, КЗ (Казахстан!), ЮГ, БЕЛ (Белоруссия!)

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Зависимости

```
pandas
openpyxl
python-telegram-bot
```

⚠️ **ВАЖНО:** Версии НЕ зафиксированы (нет requirements.txt)

### Входные данные (каждый понедельник)

```
input/
├── Отчет по приросту регионы/
│   └── По регионам.xlsx
├── Отчет по приросту аксессуаров по магазинам/
│   └── Рассылка аксессуары магазины.xlsx
└── Обувь остатки и оборачиваемость по группам товара/
    └── Отчет по оборачиваемости ТЗ регион ННВ.xlsx
```

### Выходные файлы

```
output/
├── dashboard_current.html  ← PRODUCTION (отправляется в Telegram)
└── archive/                ← История старых версий
    └── 2026-01-23_10-11-29.html
```

### Логи

```
logs/
├── outlook_macro_YYYYMMDD.log  ← VBA макрос
└── telegram_send_YYYYMMDD.log  ← Telegram отправка
```

---

## 🚨 КРИТИЧНЫЕ ОСОБЕННОСТИ

### Секреты (НЕ КОММИТИТЬ!)

Файл `telegram_bot/config.py` содержит:
- `BOT_TOKEN` → Telegram bot token
- `GROUP_CHAT_ID` → ID группы для рассылки
- `RECIPIENTS` → Список chat_id получателей

⚠️ **config.py в .gitignore** → не попадает в git

### Mobile-first дизайн дашборда

- Целевое устройство: iPhone в Telegram браузере
- ❌ НЕ использовать: Chart.js, Plotly (не рендерятся в Telegram iOS)
- ✅ Использовать: HTML progress bars, таблицы, flexbox
- Максимум 5 колонок в таблице (иначе не влезает на экране)

### Терминология

- НЕ "РОП" → ИСПОЛЬЗУЙ "директор подразделения"
- БЕЛ = Белоруссия (НЕ Белгород!)
- КЗ = Казахстан (НЕ Казань!)
- ОБУВЬ измеряется в ПАРАХ
- АКСЕССУАРЫ измеряются в ШТУКАХ

---

## 📅 ГРАФИК РАБОТЫ

**Production цикл:**
- **Понедельник утром:** Приходят письма с отчётами
- **Понедельник 20:00:** Outlook макрос сохраняет файлы → Python генерирует дашборд → Telegram отправляет в группу
- **Вторник-воскресенье:** Система в standby режиме

**Следующий production run:** 09.02.2026 (ПН) 20:00

---

## 🔄 ТЕКУЩАЯ ЗАДАЧА (05.02.2026)

### IT-Review и Рефакторинг

**Цель:** Привести проект к стандартам ИТ-департамента Ecosoft

**Дедлайн:** 08.02.2026 23:00 (до production run 09.02)

**Критичные задачи:**
1. ✅ Секреты в `.env` (убрать хардкод)
2. ✅ Структурированное логирование (JSON format)
3. ✅ Error handling + retry механизм
4. ✅ `requirements.txt` с зафиксированными версиями
5. ✅ Валидация входных данных
6. ✅ Документация (docstrings, CHANGELOG.md)

**Важно:** Рефакторинг в отдельной ветке `feature/it-compliance-refactoring`

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ

Читай обязательно:
- `PROJECT_CONTEXT_KARI_DASHBOARD.md` → Полный контекст проекта
- `README.md` → Описание и quick start
- `QUICK_START_GUIDE.md` → Пошаговая инструкция запуска

---

## ⚠️ ВАЖНЫЕ НАПОМИНАНИЯ

1. **НЕ СЛОМАЙ PRODUCTION!** Все изменения через feature branch
2. **НЕ КОММИТЬ СЕКРЕТЫ!** Проверяй .gitignore перед push
3. **НЕ ВЫДУМЫВАЙ ДАННЫЕ!** Только реальные из Excel
4. **ТЕСТИРУЙ НА iPhone!** Дашборд должен работать в Telegram iOS
5. **ДОКУМЕНТИРУЙ ВСЁ!** Каждое изменение в WORK_LOG.md

---

## 🤝 КОНТАКТЫ

**Владелец проекта:** Дмитрий Сальников  
**Должность:** Региональный директор ННВ, KARI  
**Локация:** Казань, Россия  
**Часовой пояс:** MSK (UTC+3)

---

**Версия документа:** 1.0  
**Дата создания:** 05.02.2026  
**Последнее обновление:** 05.02.2026
