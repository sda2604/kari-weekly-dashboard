# ИНСТРУКЦИЯ: Использование системы версионирования

## ✅ Что уже сделано

1. ✅ Создан дашборд: `output/dashboard_current.html` (26 KB)
2. ✅ Создан скрипт версионирования для Linux/Mac: `code/update_dashboard.sh`
3. ✅ Создан скрипт версионирования для Windows: `update_dashboard.bat`
4. ✅ Создан скрипт очистки старых файлов: `cleanup_output.bat`

## 📂 Структура проекта (после очистки)

```
2025-01-22_Автоматизация_еженедельных_отчетов/
├── input/                          ← Исходные Excel файлы
├── output/                         ← РАБОЧАЯ ПАПКА
│   ├── dashboard_current.html      ← ТЕКУЩАЯ версия
│   └── archive/                    ← ИСТОРИЯ (макс 10 версий)
├── code/                           ← Скрипты
│   └── update_dashboard.sh         ← Версионирование (Linux/Mac)
├── update_dashboard.bat            ← Версионирование (Windows)
└── cleanup_output.bat              ← Очистка старых файлов
```

## 🚀 Быстрый старт

### Шаг 1: Очистка старых файлов (один раз)

**Windows:**
```
Запустить: cleanup_output.bat
```

Это удалит все старые файлы из `output/`, оставив только:
- `dashboard_current.html` (текущая версия)
- `archive/` (папка для истории)

### Шаг 2: Использование версионирования

**ПЕРЕД созданием нового дашборда ВСЕГДА запускай:**

**Windows:**
```
Запустить: update_dashboard.bat
```

**Linux/Mac/Git Bash:**
```bash
bash code/update_dashboard.sh
```

**Что делает скрипт:**
1. Перемещает `dashboard_current.html` → `archive/2026-01-23_14-30.html`
2. Удаляет версии старше 10-й
3. Освобождает место для нового `dashboard_current.html`

### Шаг 3: Создание нового дашборда

После версионирования создавай новый дашборд:
- ВСЕГДА сохраняй как `output/dashboard_current.html`
- НИКОГДА не создавай файлы типа `dashboard_v2.html`, `dashboard_final.html` и т.д.

## ⚠️ КРИТИЧНЫЕ ПРАВИЛА

### ✅ ПРАВИЛЬНО:
```
output/
├── dashboard_current.html    ← ЕДИНСТВЕННЫЙ файл в output/
└── archive/
    ├── 2026-01-23_14-30.html
    └── 2026-01-23_10-49.html
```

### ❌ НЕПРАВИЛЬНО:
```
output/
├── dashboard_current.html
├── dashboard_v2.html         ← ХАОС!
├── dashboard_final.html      ← ХАОС!
└── dashboard_NEW.html        ← ХАОС!
```

## 📋 Примеры использования

### Пример 1: Обновление дашборда

```batch
REM 1. Версионировать старый
update_dashboard.bat

REM 2. Создать новый
python generate_dashboard.py

REM 3. Сохранить как dashboard_current.html
```

### Пример 2: Просмотр истории

```batch
REM Посмотреть все версии
dir output\archive

REM Открыть старую версию
start output\archive\2026-01-23_14-30.html
```

## 🔍 Проверка

Проверить что всё правильно:

```batch
REM В output/ должен быть ТОЛЬКО 1 HTML файл
dir output\*.html

REM Должен показать только: dashboard_current.html
```

## 📊 Преимущества системы

✅ Всегда понятно какой файл актуальный  
✅ История сохраняется автоматически  
✅ Нет хаоса с именами файлов  
✅ Можно откатиться к старой версии  
✅ Профессиональный подход  

## 💡 Философия

**Вопрос:** "Какой дашборд актуальный?"  
**Ответ:** `output/dashboard_current.html`

**ВСЕГДА. БЕЗ ИСКЛЮЧЕНИЙ.**

---

**Дата создания:** 23 января 2026  
**Статус:** ГОТОВО К ИСПОЛЬЗОВАНИЮ  
