# ЗАДАЧА: ВЫЛОЖИТЬ ПРОЕКТ KARI DASHBOARD НА GITHUB

## ЧТО СДЕЛАТЬ
Создать публичный GitHub репозиторий `kari-weekly-dashboard` и залить туда код проекта автоматизации еженедельных отчётов KARI.

## ВАЖНО: БЕЗОПАСНОСТЬ
⚠️ НЕ ЗАЛИВАТЬ файл `telegram_bot/config.py` — он содержит токен бота и chat_id!
Вместо него создать `telegram_bot/config.example.py` с placeholder-ами.

## СТРУКТУРА РЕПОЗИТОРИЯ

```
kari-weekly-dashboard/
├── README.md                    # Описание проекта (на русском)
├── .gitignore                   # Игнорировать config.py, логи, данные
├── generate_dashboard.py        # Главный генератор дашборда
├── run_full_pipeline.bat        # Запуск полного цикла (Windows)
├── OUTLOOK_VBA_v5.txt          # VBA макрос для Outlook v5.0
├── telegram_bot/
│   ├── send_dashboard.py       # Отправка в Telegram
│   ├── period_parser.py        # Парсинг периода из Excel
│   └── config.example.py       # ПРИМЕР конфига (без токена!)
├── test/
│   ├── test_full_pipeline.py   # Тест всей цепочки
│   └── RUN_TEST.bat            # Запуск теста
└── docs/
    └── ARCHITECTURE.md          # Схема работы системы
```

## ФАЙЛЫ ДЛЯ ЗАЛИВКИ

### 1. Из директории проекта (C:\Users\salni\Desktop\Данные для Claude\WORK\2025-01-22_Автоматизация_еженедельных_отчетов\):

| Файл | Путь в репо |
|------|-------------|
| generate_dashboard.py | generate_dashboard.py |
| run_full_pipeline.bat | run_full_pipeline.bat |
| OUTLOOK_VBA_v5.txt | OUTLOOK_VBA_v5.txt |
| telegram_bot/send_dashboard.py | telegram_bot/send_dashboard.py |
| telegram_bot/period_parser.py | telegram_bot/period_parser.py |
| test/test_full_pipeline.py | test/test_full_pipeline.py |
| test/RUN_TEST.bat | test/RUN_TEST.bat |
| .gitignore | .gitignore |

### 2. СОЗДАТЬ НОВЫЕ ФАЙЛЫ:

#### telegram_bot/config.example.py
```python
# Telegram Bot Configuration
# Скопируй этот файл как config.py и заполни реальные значения

BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"  # Получить у @BotFather

RECIPIENTS = [
    123456789,    # chat_id получателя
]

USE_GROUP = True
GROUP_CHAT_ID = -1001234567890  # ID группы Telegram

DASHBOARD_PATH = "../output/dashboard_current.html"

MESSAGE_TEMPLATE = """
📊 <b>Недельный отчёт ННВ по товару</b>

Период: {period}
Регион: Нижний Новгород

📎 Дашборд во вложении

---
<i>Автоматическая рассылка | {timestamp}</i>
"""
```

#### README.md (для GitHub)
```markdown
# KARI Weekly Dashboard 📊

Автоматизация еженедельных отчётов для розничной сети KARI (регион ННВ, 119 магазинов).

## Что делает система

Каждый понедельник автоматически:
1. **Outlook VBA макрос** перехватывает письма с отчётами и сохраняет вложения
2. **Python скрипт** обрабатывает 3 Excel-файла и генерирует HTML дашборд
3. **Telegram бот** рассылает дашборд директорам подразделений

## Результат

| Метрика | Было | Стало |
|---------|------|-------|
| Время обработки | 8 часов | 0 минут |
| Формат | 15+ Excel файлов | 1 мобильный дашборд |
| Доставка | Email (никто не читает) | Telegram (5 мин на чтение) |

**Экономия: 384 часа/год**

## Архитектура

```
Outlook (VBA макрос v5.0)
  → Сохраняет 3 Excel из почты в input/
  
Windows Task Scheduler (пн 20:00)
  → Запускает run_full_pipeline.bat
    → generate_dashboard.py (Python + pandas)
      → Читает Excel → Анализирует → HTML дашборд
    → send_dashboard.py (Telegram Bot API)
      → Отправляет дашборд в группу
```

## Структура дашборда

1. **ТОП-3 действия** — что делать прямо сейчас
2. **Компания и регионы** — позиция ННВ среди 11 регионов
3. **Подразделения ННВ** — 7 подразделений, КОП, рост
4. **Ключевые категории обуви** — ТОП-5 по доле (70%+ оборота)
5. **Категории с ростом и дефицитом** — упущенная выгода
6. **Неликвидные остатки** — заморожен капитал
7. **Дисбалансы** — где затовар vs дефицит, план перемещений
8. **Магазины** — лучшие и худшие
9. **Аксессуары** — отдельный анализ
10. **План действий на неделю**

## Технологии

- **Python 3.14** + pandas + openpyxl — обработка Excel
- **python-telegram-bot** — рассылка
- **Outlook VBA** — автосохранение вложений
- **Windows Task Scheduler** — автозапуск
- **HTML/CSS** — мобильный дашборд (iPhone + Telegram)

## Установка

### 1. Python зависимости
```bash
pip install pandas openpyxl requests python-telegram-bot
```

### 2. Telegram бот
```bash
cp telegram_bot/config.example.py telegram_bot/config.py
# Заполнить BOT_TOKEN и GROUP_CHAT_ID
```

### 3. Outlook макрос
Установить код из `OUTLOOK_VBA_v5.txt` в Outlook:
Alt+F11 → ThisOutlookSession → вставить код → Ctrl+S

### 4. Task Scheduler
Создать задачу на запуск `run_full_pipeline.bat` каждый понедельник в 20:00.

## Тестирование
```bash
python test/test_full_pipeline.py
```

## Автор

**Дмитрий Сальников** — Региональный директор ННВ, KARI

Разработано с помощью Claude (Anthropic) | Январь 2026
```

#### docs/ARCHITECTURE.md
```markdown
# Архитектура системы KARI Weekly Dashboard

## Схема потока данных

```
[Корпоративная почта] → reports@kari.com отправляет 3 типа отчётов по понедельникам
         │
         ▼
[Outlook VBA макрос v5.0]
   │  Фильтрует по теме письма (CyrInStr — кириллико-безопасный InStr)
   │  Сохраняет вложения (.xlsx, .pdf) в input/
   │  Логирует в logs/outlook_macro_YYYYMMDD.log
   │
   ▼
[input/] — 3 Excel файла:
   ├── Отчет по приросту регионы/По регионам.xlsx
   ├── Отчет по приросту аксессуаров по магазинам/Рассылка аксессуары магазины.xlsx
   └── Обувь остатки и оборачиваемость по группам товара/Отчет по оборачиваемости ТЗ регион ННВ.xlsx
         │
         ▼
[Windows Task Scheduler] → Запуск пн 20:00 МСК
         │
         ▼
[run_full_pipeline.bat]
   │
   ├── [generate_dashboard.py]
   │      Читает Excel (pandas + openpyxl)
   │      Анализирует: КОП, рост, дисбалансы
   │      Генерирует HTML дашборд (~34 KB)
   │      Сохраняет: output/dashboard_current.html
   │
   └── [telegram_bot/send_dashboard.py]
          Парсит период из Excel (period_parser.py)
          Отправляет HTML файл в Telegram группу
          Логирует результат
         │
         ▼
[Telegram группа]
   └── Директора подразделений читают дашборд с телефонов
```

## Ключевые метрики анализа

- **КОП** (Коэффициент Остатки/Продажи) = Доля в остатках % / Доля в продажах %
  - КОП 0.8-1.2 = баланс (зелёный)
  - КОП 1.2-2.0 = внимание (оранжевый)
  - КОП >2.0 = затоваривание (красный)
  - КОП <0.8 = дефицит (красный)

- **Оборачиваемость** — недели на складе (>50 = неликвид)
- **Доля в продажах** — вклад категории в оборот региона

## Совместимость

- HTML дашборд оптимизирован под iPhone + Telegram in-app browser
- Без Chart.js/D3.js (не работают в Telegram iOS)
- Inline CSS/JS, размер <50 KB
- Flexbox layout, адаптивный дизайн
```

## GITHUB USERNAME
Спросить у Дмитрия его GitHub username перед созданием репозитория.
Если нет аккаунта — создать репозиторий через GitHub API от имени подключённого аккаунта.

## ПОСЛЕ СОЗДАНИЯ РЕПОЗИТОРИЯ
Сообщить ссылку: https://github.com/{username}/kari-weekly-dashboard
Эта ссылка пойдёт в AI-Progress отчёт как доказательство.
