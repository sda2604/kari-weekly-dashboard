# 🚀 БЫСТРЫЙ СТАРТ: Telegram Bot для рассылки дашборда

**Время настройки:** 10-15 минут  
**Дата:** 23 января 2026

---

## ✅ ЧТО СОЗДАНО

1. **Скрипты:**
   - `telegram_bot/send_dashboard.py` - рассылка дашборда
   - `telegram_bot/get_chat_id.py` - получение chat_id пользователей
   - `telegram_bot/config.py` - конфигурация (ТОКЕН И ПОЛУЧАТЕЛИ)

2. **.bat файлы (для Windows):**
   - `send_telegram.bat` - запуск рассылки
   - `get_telegram_ids.bat` - получение chat_id

3. **Документация:**
   - `TELEGRAM_BOT_SETUP.md` - полная инструкция

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### ШАГ 1: Создай бота (5 минут)

1. Открой Telegram
2. Найди: `@BotFather`
3. Отправь: `/newbot`
4. Имя: `KARI Dashboard Bot`
5. Username: `kari_dashboard_nnv_bot` (или другой доступный)
6. **Скопируй токен** (вида: `123456789:ABCdef...`)

---

### ШАГ 2: Настрой config.py (2 минуты)

1. Открой файл: `telegram_bot/config.py`
2. Замени `"ЗАМЕНИ_НА_СВОЙ_ТОКЕН"` на свой токен
3. Сохрани файл

**Пример:**
```python
BOT_TOKEN = "123456789:ABCdefGHIjklMNOpqrsTUVwxyz"
```

---

### ШАГ 3: Установи библиотеку (1 минута)

Открой командную строку (cmd) и выполни:

```bash
pip install python-telegram-bot
```

---

### ШАГ 4: Получи chat_id (5 минут)

**Вариант A: Для себя**

1. Запусти: `get_telegram_ids.bat`
2. Открой своего бота в Telegram
3. Отправь: `/start`
4. Бот покажет твой chat_id
5. Скопируй chat_id
6. Добавь в `config.py`:

```python
RECIPIENTS = [
    123456789,    # Дмитрий Сальников
]
```

**Вариант B: Для группы**

1. Создай группу в Telegram
2. Добавь бота в группу (как администратора)
3. Отправь в группу: `/start`
4. Скопируй group_chat_id (начинается с `-`)
5. В `config.py` измени:

```python
USE_GROUP = True
GROUP_CHAT_ID = -1001234567890
```

---

### ШАГ 5: Тестовая рассылка (1 минута)

1. Запусти: `send_telegram.bat`
2. Дашборд отправится всем получателям
3. Проверь Telegram

---

## ✅ ГОТОВО!

Теперь можно:
- ✅ Отправлять дашборд одной командой
- ✅ Настроить автоматическую рассылку (через Task Scheduler)

---

## 🔧 ИСПОЛЬЗОВАНИЕ

### Ручная рассылка:

```batch
send_telegram.bat
```

### Добавить нового получателя:

1. Попроси отправить `/start` боту
2. Запусти `get_telegram_ids.bat`
3. Скопируй chat_id
4. Добавь в `config.py` в список `RECIPIENTS`

---

## ⚠️ ВАЖНО

1. **НЕ публикуй токен бота** (он уже в .gitignore)
2. **Проверь config.py** перед первой отправкой
3. **Дашборд должен существовать:** `output/dashboard_current.html`

---

## 🐛 ПРОБЛЕМЫ И РЕШЕНИЯ

### Ошибка: "ModuleNotFoundError: No module named 'telegram'"

**Решение:**
```bash
pip install python-telegram-bot
```

### Ошибка: "Unauthorized"

**Решение:** Проверь токен в `config.py`

### Ошибка: "Chat not found"

**Решение:** Пользователь должен сначала отправить `/start` боту

### Дашборд не найден

**Решение:** Создай дашборд: `output/dashboard_current.html`

---

## 📞 КОМАНДЫ БОТА

Пользователи могут отправлять боту:
- `/start` - регистрация и получение chat_id
- `/help` - помощь
- `/latest` - запрос последнего дашборда (можно добавить позже)

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

После настройки Telegram бота:
1. ✅ Task Scheduler - автоматическая рассылка по понедельникам
2. ✅ Outlook правило - автосохранение вложений
3. ✅ Python автоматизация - генерация дашборда из Excel

---

**Полная инструкция:** `TELEGRAM_BOT_SETUP.md`  
**Создано:** Claude.ai  
**Версия:** 1.0
