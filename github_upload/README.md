# KARI Weekly Dashboard 📊

Автоматизация еженедельных отчётов для розничной сети KARI (регион ННВ, 119 магазинов).

## Что делает система

Каждый понедельник автоматически:
1. **Outlook VBA макрос** перехватывает письма с отчётами и сохраняет вложения
2. **Python скрипт** обрабатывает 3 Excel-файла и генерирует HTML дашборд
3. **Telegram бот** рассылает дашборд директорам подразделений

## Результат

| Метрика | Было | Стало |
|---------|------|-------|
| Время обработки | 8 часов | 0 минут (автоматически) |
| Формат | 15+ Excel файлов | 1 мобильный HTML дашборд |
| Доставка | Email (никто не читает) | Telegram (5 мин на чтение) |
| Охват анализа | 0% отчётов | 100% отчётов |

**Экономия: 384 часа/год (~570,000₽/год)**

## Архитектура

```
[Корпоративная почта reports@kari.com]
         │
         ▼
[Outlook VBA макрос v5.0]
   │  Фильтрует по теме (CyrInStr — кириллико-безопасный)
   │  Сохраняет .xlsx/.pdf в input/
   │  Логирует в logs/
   │
   ▼
[Windows Task Scheduler — пн 20:00 МСК]
   │
   ▼
[run_full_pipeline.bat]
   │
   ├── generate_dashboard.py (Python + pandas)
   │     Читает 3 Excel → Анализ → HTML дашборд (~34 KB)
   │
   └── telegram_bot/send_dashboard.py
         Отправляет в Telegram группу
         │
         ▼
[Директора подразделений читают с телефонов]
```

## Входные данные (3 Excel файла еженедельно)

| Файл | Содержание | Приоритет |
|------|------------|----------|
| По регионам.xlsx | Продажи обуви по 11 регионам, динамика | 1 |
| Рассылка аксессуары магазины.xlsx | Продажи аксессуаров по 119 магазинам | 2 |
| Отчет по оборачиваемости ТЗ регион ННВ.xlsx | Остатки, КОП, оборачиваемость | 3 |

## Структура дашборда

1. **⚡ ТОП-3 действия** — что делать прямо сейчас
2. **🏢 Компания и регионы** — позиция ННВ среди 11 регионов
3. **📊 Подразделения ННВ** — 7 подразделений, КОП, рост
4. **💰 Ключевые категории обуви** — ТОП-5 по доле (70%+ оборота)
5. **⭐ Категории с ростом и дефицитом** — упущенная выгода
6. **💀 Неликвидные остатки** — заморожен капитал
7. **📍 Дисбалансы** — затовар vs дефицит, план перемещений
8. **🏪 Магазины** — лучшие и худшие
9. **👜 Аксессуары** — отдельный анализ
10. **📋 План действий на неделю**

## Ключевые метрики

- **КОП** (Коэффициент Остатки/Продажи) = Доля в остатках % / Доля в продажах %
  - 0.8–1.2 = баланс ✅
  - 1.2–2.0 = внимание ⚠️
  - более 2.0 = затоваривание 🔴
  - менее 0.8 = дефицит 🔴
- **Оборачиваемость** — недели на складе (более 50 = неликвид)
- **Доля в продажах** — вклад категории в оборот

## Технологии

- **Python 3** + pandas + openpyxl — обработка Excel
- **python-telegram-bot** — рассылка
- **Outlook VBA** — автосохранение вложений (v5.0 с CyrInStr)
- **Windows Task Scheduler** — автозапуск
- **HTML/CSS** — мобильный дашборд (iPhone + Telegram)

## Установка

### 1. Python зависимости
```bash
pip install pandas openpyxl requests python-telegram-bot
```

### 2. Telegram бот
```bash
cp telegram_bot/config.example.py telegram_bot/config.py
# Заполнить BOT_TOKEN и GROUP_CHAT_ID
```

### 3. Outlook макрос
Установить код из `outlook_vba/macro_v5.txt` в Outlook:
```
Alt+F11 → ThisOutlookSession → вставить код → Ctrl+S → перезапустить Outlook
```

### 4. Task Scheduler
```
Создать задачу → Триггер: понедельник 20:00 → Действие: run_full_pipeline.bat
```

## Тестирование
```bash
python test/test_full_pipeline.py
```

## Структура проекта

```
kari-weekly-dashboard/
├── generate_dashboard.py        # Главный генератор дашборда
├── run_full_pipeline.bat        # Запуск полного цикла
├── outlook_vba/
│   └── macro_v5.txt             # VBA макрос для Outlook
├── telegram_bot/
│   ├── send_dashboard.py        # Отправка в Telegram
│   ├── period_parser.py         # Парсинг периода из Excel
│   └── config.example.py        # Пример конфигурации
├── test/
│   ├── test_full_pipeline.py    # Тест всей цепочки
│   └── RUN_TEST.bat             # Запуск теста (Windows)
├── docs/
│   └── ARCHITECTURE.md          # Детальная архитектура
├── input/                       # Excel файлы (не в репо)
├── output/                      # Дашборды (не в репо)
└── logs/                        # Логи (не в репо)
```

## Совместимость дашборда

- ✅ iPhone Safari (Telegram in-app browser)
- ✅ Android Chrome
- ✅ Desktop (Windows, Mac)
- ✅ Размер менее 50 KB (быстрая загрузка)
- ❌ Без Chart.js/D3.js (не работают в Telegram iOS)
- ✅ Inline CSS/JS (один файл)

## Автор

**Дмитрий Сальников** — Региональный директор ННВ, KARI

Разработано с помощью Claude (Anthropic) | Январь-Февраль 2026

## Лицензия

MIT
